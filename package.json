{
  "name": "akashnaren.github.io",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/restore-shell.mjs && vite",
    "build": "node scripts/restore-shell.mjs && tsc && vite build && node scripts/publish-pages.mjs",
    "preview": "vite preview",
    "test": "node scripts/check-dist.mjs"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.2.2"
  }
}
